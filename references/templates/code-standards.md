# 代码规范 (L1)

> 本文件定义项目的编码标准。所有 Dev 视角的编码行为和 QA 视角的审查行为以此为基准。
> 修改频率：中低。由 Architect 视角在规范迭代（模式四）时更新。
> Last Updated: {日期}

## 命名规范

| 对象 | 规则 | 示例 |
|------|------|------|
| 文件名 | {kebab-case / camelCase / ...} | {user-service.ts} |
| 函数名 | {camelCase / snake_case / ...} | {getUserById} |
| 类名 | {PascalCase} | {UserService} |
| 常量 | {UPPER_SNAKE_CASE} | {MAX_RETRY_COUNT} |
| 组件名 | {PascalCase} | {UserProfileCard} |
| CSS 类 | {BEM / tailwind / ...} | {.user-card__title} |

## 文件组织

- {规则 1 — 例如：每个模块一个目录，包含 index 导出文件}
- {规则 2 — 例如：类型定义放在 types.ts 中}
- {规则 3 — 例如：工具函数放在 utils/ 下}

## 错误处理

- {规则 — 例如：所有异步操作必须有错误处理}
- {规则 — 例如：使用统一的错误类型定义}
- {规则 — 例如：用户可见的错误信息不暴露内部细节}

## 注释与文档

- {规则 — 例如：公共函数必须有 JSDoc/docstring}
- {规则 — 例如：复杂逻辑用行内注释解释"为什么"而非"做了什么"}
- {规则 — 例如：TODO 注释必须关联 issue 编号}

## 禁止的 Pattern

> QA 视角发现以下 pattern 时必须标记为阻塞性问题

- {禁止 1 — 例如：禁止使用 any 类型}
- {禁止 2 — 例如：禁止在组件中直接调用 API}
- {禁止 3}

## 推荐的 Pattern

> 非强制，但鼓励使用

- {推荐 1}
- {推荐 2}

## 测试规范

- {规则 — 例如：新功能必须包含单元测试}
- {规则 — 例如：测试文件与源文件同目录，命名为 xxx.test.ts}

## 变更记录

| 日期 | 变更内容 | 原因 |
|------|---------|------|
| {日期} | 初始化 | 项目启动 |
