# 系统架构文档 (L1)

> 本文件描述系统的整体架构。Architect 视角主要维护。
> 修改频率：中。架构调整或新模块引入时更新。
> Last Updated: {日期}

## 架构概览

{一段话描述系统的整体架构风格和核心思路}

## 技术栈

| 层次 | 技术选型 | 选型理由 |
|------|---------|---------|
| 前端 | {框架/库} | {为什么选它} |
| 后端 | {框架/库} | {为什么选它} |
| 数据库 | {类型/产品} | {为什么选它} |
| 部署 | {方式} | {为什么选它} |
| 其他 | {如有} | {如有} |

## 模块划分

```
{用文本描述模块结构，例如：}

src/
├── modules/
│   ├── auth/          # 用户认证与授权
│   ├── user/          # 用户管理
│   ├── ...
├── shared/            # 跨模块共享代码
├── infra/             # 基础设施层（数据库、缓存、消息队列）
└── ...
```

### 模块职责

| 模块 | 职责 | 对外接口 | 依赖的模块 |
|------|------|---------|-----------|
| {模块名} | {一句话职责} | {主要暴露什么} | {依赖谁} |

## 模块间通信规则

- {规则 1 — 例如：模块间通过接口通信，不直接访问内部实现}
- {规则 2 — 例如：共享代码放在 shared/ 下，不允许模块间直接引用}
- {规则 3}

## 数据流

{描述核心数据流向，如：用户请求 → API 层 → 业务模块 → 数据层}

## 分层原则

{描述系统的分层策略和每层的职责边界}

## 变更记录

| 日期 | 变更内容 | 关联 PRD/决策 |
|------|---------|-------------|
| {日期} | 初始化 | 项目启动 |
